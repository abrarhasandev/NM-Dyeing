# অ্যান্ড্রয়েড অ্যাপ ইন্টিগ্রেশন গাইডলাইন (NM-Dyeing)

এই গাইডলাইনটিতে NM-Dyeing প্রজেক্টের সাথে ট্রান্সপোর্ট এমপ্লয়ি (Transport Employee) অ্যান্ড্রয়েড অ্যাপ্লিকেশনের কানেকশন, এপিআই এন্ডপয়েন্ট (Endpoints) এবং ডেটা আদান-প্রদান সম্পর্কে বিস্তারিত আলোচনা করা হলো।

আমাদের প্রজেক্টের ব্যাকএন্ড হিসেবে **Convex** ব্যবহার করা হয়েছে। তাই অ্যান্ড্রয়েড অ্যাপ থেকে সাধারণ REST API-এর (যেমন: Retrofit/Volley) বদলে **Convex Android SDK** ব্যবহার করে ডেটা আদান-প্রদান করতে হবে।

---

## ১. কিভাবে রিকোয়েস্ট পাঠাতে হবে? (Convex SDK Setup)

অ্যান্ড্রয়েড অ্যাপে Convex এর সাথে কানেক্ট করতে আপনার প্রজেক্টের `build.gradle` ফাইলে নিচের ডিপেন্ডেন্সি যুক্ত করতে হবে:

```gradle
implementation("dev.convex:convex-android:0.1.0") // লেটেস্ট ভার্সন চেক করে নিবেন
```

এরপর অ্যাপ্লিকেশনের শুরুতে (Application class বা DI module-এ) Convex Client ইনিশিয়ালাইজ করতে হবে:
```kotlin
val convexClient = ConvexClient("https://<আপনার_CONVEX_URL>.convex.cloud")
```

Convex-এ মূলত দুই ধরনের রিকোয়েস্ট পাঠানো হয়:
1. **Mutation:** ডেটা সেভ, আপডেট বা ডিলিট করার জন্য (যেমন: Login, Logout)। ഇതിൽ `convexClient.mutation()` ব্যবহার করা হয়।
2. **Query (Subscription):** ডেটা দেখার জন্য বা রিয়েল-টাইম ডেটা পাওয়ার জন্য। এতে `convexClient.subscribe()` ব্যবহার করা হয়। ডেটাবেজে কোনো পরিবর্তন হলে অ্যাপে সাথে সাথে আপডেট হয়ে যাবে।

---

## ২. কোন কোন Endpoint-এ রিকোয়েস্ট পাঠাবে এবং কী কী তথ্য দিতে হবে?

অ্যান্ড্রয়েড অ্যাপ্লিকেশন থেকে নিচের Endpoint গুলোতে রিকোয়েস্ট পাঠাতে হবে। প্রতিটা রিকোয়েস্টের ধরণ এবং প্রয়োজনীয় প্যারামিটার (Arguments) নিচে বিস্তারিত দেওয়া হলো:

### ২.১ লগইন করা (Login)
অ্যাপে প্রবেশ করার জন্য এমপ্লয়িকে তার `loginId` এবং `password` দিতে হবে।

* **Endpoint Name:** `androidAuth:login`
* **Type:** Mutation
* **অ্যান্ড্রয়েড অ্যাপ থেকে যে তথ্যগুলো পাঠাতে হবে (Arguments):**
  * `loginId` (String): এমপ্লয়ির লগইন আইডি।
  * `password` (String): এমপ্লয়ির পাসওয়ার্ড।
  * `deviceInfo` (String, ঐচ্ছিক): ডিভাইসের নাম বা মডেল (যেমন: "Pixel 7 Pro")।
  * `fcmToken` (String, ঐচ্ছিক): পুশ নোটিফিকেশনের জন্য Firebase Cloud Messaging (FCM) টোকেন।
* **ব্যাকএন্ড থেকে যা রিটার্ন আসবে:**
  সফলভাবে লগইন হলে একটি `token` সহ কিছু প্রাথমিক তথ্য পাওয়া যাবে।
  ```json
  {
    "token": "a1b2c3d4...", 
    "employeeId": "jd1...", 
    "name": "Abul Kashem", 
    "vehicleType": "Power Ven - 35W"
  }
  ```
> **গুরুত্বপূর্ণ:** লগইনের পর প্রাপ্ত `token` টি অ্যান্ড্রয়েড অ্যাপের লোকাল স্টোরেজে (EncryptedSharedPreferences-এ) খুব সুরক্ষিতভাবে সেভ করে রাখতে হবে। কারণ পরবর্তীতে অন্য যেকোনো রিকোয়েস্ট পাঠাতে এই টোকেনটি অবশ্যই লাগবে।

### ২.২ সেশন চেক করা (Validate Session)
অ্যাপ চালু হওয়ার সময় ইউজার আগে থেকেই লগইন করা আছে কিনা তা চেক করতে এটি ব্যবহার করা হয়।

* **Endpoint Name:** `androidAuth:validateSession`
* **Type:** Query
* **অ্যান্ড্রয়েড অ্যাপ থেকে যে তথ্যগুলো পাঠাতে হবে (Arguments):**
  * `token` (String): লগইনের সময় সেভ করে রাখা টোকেন।
* **ব্যাকএন্ড থেকে যা রিটার্ন আসবে:**
  টোকেনটি সঠিক থাকলে `{ "employeeId": "..." }` আসবে। আর টোকেনের মেয়াদ শেষ হয়ে গেলে বা ভুল হলে `null` আসবে। `null` আসলে অ্যাপ থেকে ইউজারকে লগআউট করে লগইন স্ক্রিনে পাঠাতে হবে।

### ২.৩ প্রোফাইল ইনফরমেশন দেখা (Get Profile)
লগইন করা এমপ্লয়ির বিস্তারিত তথ্য (নাম, ঠিকানা, ফোন নম্বর, গাড়ির ধরন ইত্যাদি) দেখার জন্য।

* **Endpoint Name:** `androidApi:getProfile`
* **Type:** Query / Subscription
* **অ্যান্ড্রয়েড অ্যাপ থেকে যে তথ্যগুলো পাঠাতে হবে (Arguments):**
  * `token` (String): লগইনের সময় সেভ করে রাখা টোকেন।
* **ব্যাকএন্ড থেকে যা রিটার্ন আসবে:**
  এমপ্লয়ির পুরো প্রোফাইল অবজেক্ট (পাসওয়ার্ড বাদে)। যেমন: `name`, `phoneNumbers`, `address`, `dob`, `age`, `vehicleType`, `vehicleWheels`, `clothCapacityYards`, `avatar` ইত্যাদি।

### ২.৪ নিজের কাজ বা অর্ডারগুলো দেখা (Get Assigned Orders)
এমপ্লয়িকে যেসব ট্রান্সপোর্ট অর্ডার অ্যাসাইন করা হয়েছে তার তালিকা রিয়েল-টাইমে দেখার জন্য।

* **Endpoint Name:** `androidApi:getMyOrders`
* **Type:** Query / Subscription
* **অ্যান্ড্রয়েড অ্যাপ থেকে যে তথ্যগুলো পাঠাতে হবে (Arguments):**
  * `token` (String): লগইনের সময় সেভ করে রাখা টোকেন।
* **ব্যাকএন্ড থেকে যা রিটার্ন আসবে:**
  একটি অ্যারে/লিস্ট আসবে, যেখানে প্রতিটি অর্ডারের বিস্তারিত তথ্য থাকবে। যেমন: `transporterName`, `displayOrderId`, `companyName`, `totalGoj`, `status`, `date` ইত্যাদি। নতুন অর্ডার আসলে বা স্ট্যাটাস পরিবর্তন হলে লিস্টটি নিজে নিজেই অ্যাপে আপডেট হবে।

### ২.৫ বিলিং হিস্ট্রি দেখা (Get Billing History)
এমপ্লয়ির বিলের অবস্থা এবং হিস্ট্রি দেখার জন্য।

* **Endpoint Name:** `androidApi:getMyBills`
* **Type:** Query / Subscription
* **অ্যান্ড্রয়েড অ্যাপ থেকে যে তথ্যগুলো পাঠাতে হবে (Arguments):**
  * `token` (String): লগইনের সময় সেভ করে রাখা টোকেন।
* **ব্যাকএন্ড থেকে যা রিটার্ন আসবে:**
  বিলের অ্যারে/লিস্ট আসবে। যেখানে `billNumber`, `totalAmount`, `status`, `date`, `orderIds` ইত্যাদি তথ্য থাকবে।

### ২.৬ FCM টোকেন আপডেট করা (Update FCM Token)
ব্যাকগ্রাউন্ডে ফায়ারবেস (Firebase) এর FCM টোকেন পরিবর্তন হয়ে গেলে সার্ভারে সেটি আপডেট করার জন্য।

* **Endpoint Name:** `androidApi:updateFCMToken`
* **Type:** Mutation
* **অ্যান্ড্রয়েড অ্যাপ থেকে যে তথ্যগুলো পাঠাতে হবে (Arguments):**
  * `token` (String): লগইনের সময় সেভ করে রাখা টোকেন।
  * `fcmToken` (String): ফায়ারবেস থেকে পাওয়া নতুন টোকেন।
* **ব্যাকএন্ড থেকে যা রিটার্ন আসবে:** `{ "success": true }`

### ২.৭ লগআউট করা (Logout)
ইউজার যখন অ্যাপ থেকে লগআউট করবে।

* **Endpoint Name:** `androidAuth:logout`
* **Type:** Mutation
* **অ্যান্ড্রয়েড অ্যাপ থেকে যে তথ্যগুলো পাঠাতে হবে (Arguments):**
  * `token` (String): বর্তমান টোকেন।
* **ব্যাকএন্ড থেকে যা রিটার্ন আসবে:** `{ "success": true }`

---

## ৩. সিকিউরিটি এবং ডেটা ফ্লো সামারি

1. **অ্যাডমিন প্যানেল:** অ্যাডমিন ড্যাশবোর্ড থেকে এমপ্লয়ির প্রোফাইল তৈরি করা হবে এবং তার `loginId` এবং `password` সেট করে দেওয়া হবে।
2. **অ্যান্ড্রয়েড অ্যাপ:** এমপ্লয়ি অ্যাপে সেই আইডি-পাসওয়ার্ড দিয়ে লগইন করবে।
3. **টোকেন:** লগইন সফল হলে একটি `token` আসবে, যা অ্যাপে সেভ রাখতে হবে।
4. **রিকোয়েস্ট:** এরপর থেকে `getProfile`, `getMyOrders`, বা `getMyBills` কল করার সময় অবশ্যই আর্গুমেন্ট হিসেবে ওই `token` টি পাঠাতে হবে।
5. **রিয়েল-টাইম:** `Query` গুলোর ক্ষেত্রে Convex SDK এর `subscribe` ফাংশন ব্যবহার করবেন। এতে করে অ্যাডমিন প্যানেল থেকে কোনো অর্ডার অ্যাসাইন করা হলে, অ্যাপ রিফ্রেশ না করেই এমপ্লয়ির স্ক্রিনে সাথে সাথে অর্ডারটি ভেসে উঠবে।
